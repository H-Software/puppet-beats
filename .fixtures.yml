fixtures:
  forge_modules:
    apt:
      ref: '2.2.2'
      repo: puppetlabs/apt
    concat:
      ref: '1.2.5'
      repo: puppetlabs/concat
    rpmkey:
      ref: '1.0.3'
      repo: stschulte/rpmkey
  repositories:
    stdlib:
      repo: "https://github.com/puppetlabs/puppetlabs-stdlib.git"
      ref: "4.25.0"
  symlinks:
    beats: "#{source_dir}"
